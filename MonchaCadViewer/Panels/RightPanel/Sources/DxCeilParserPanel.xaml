<UserControl
    x:Class="CadProjectorViewer.Panels.RightPanel.Sources.DxCeilParserPanel"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:CadProjectorViewer.Panels.RightPanel.Sources"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodel="clr-namespace:CadProjectorViewer.ViewModel"
    d:DataContext="{d:DesignInstance Type=viewmodel:DxCeilViewModel}"
    d:DesignHeight="450"
    d:DesignWidth="200"
    mc:Ignorable="d">
    <Grid>
        <DockPanel>
            <DockPanel DockPanel.Dock="Top">
                <Button
                    Margin="5"
                    Command="{Binding ClearCommand}"
                    Content="Clear"
                    DockPanel.Dock="Right" />
                <Button
                    Margin="5"
                    Command="{Binding ParseCommand}"
                    Content="Parse"
                    DockPanel.Dock="Left" />
            </DockPanel>
            <TextBox
                Margin="5"
                AcceptsReturn="True"
                DockPanel.Dock="Bottom"
                HorizontalScrollBarVisibility="Auto"
                Text="{Binding InputText, UpdateSourceTrigger=PropertyChanged}"
                TextWrapping="Wrap"
                VerticalScrollBarVisibility="Auto" />
        </DockPanel>
    </Grid>
</UserControl>
