<UserControl
    x:Class="CadProjectorViewer.Panels.ProgressPanel"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:CadProjectorViewer.Panels"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:services="clr-namespace:CadProjectorViewer.Services" 
    d:DataContext="{d:DesignInstance Type=services:Progress}"
    d:DesignHeight="150"
    d:DesignWidth="400"
    mc:Ignorable="d">
    <Grid>
        <ProgressBar Maximum="{Binding MaxValue}" Value="{Binding StatValue}" />
        <Label
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch"
            HorizontalContentAlignment="Center"
            VerticalContentAlignment="Center"
            Panel.ZIndex="99"
            Content="{Binding Label}"
            FontSize="14"
            Padding="0"
            Margin="0"/>
        <Button Foreground="Red" Background="Transparent" BorderThickness="0" FontFamily="Segoe MDL2 Assets" Content="&#xE8BB;" Width="50" HorizontalAlignment="Right" Margin="0,0,0,0"/>
    </Grid>
</UserControl>
