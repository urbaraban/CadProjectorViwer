<Window x:Class="MonchaCadViewer.DeviceManager.ProjectorView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MonchaCadViewer.DeviceManager" xmlns:canvasobj="clr-namespace:MonchaCadViewer.CanvasObj"
        mc:Ignorable="d"
        WindowState="Maximized"
        WindowStyle="None"
        Background="Black"
        Title="ProjectorView" Height="450" Width="800" KeyUp="Window_KeyUp" MouseLeftButtonDown="Window_MouseLeftButtonDown">
    <Window.Resources>
        
    </Window.Resources>
    <Window.ContextMenu>
        <ContextMenu>
            <MenuItem Header="Close" Click="CloseMenuItem_Click" />
        </ContextMenu>
    </Window.ContextMenu>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>
        <canvasobj:CadCanvas Grid.Column="0" Grid.Row="0" Grid.RowSpan="3"/>
    </Grid>
</Window>
